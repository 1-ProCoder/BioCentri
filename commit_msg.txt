feat: functional overhaul — Rules CRUD, real-data VMs, glassmorphic content, entrance animations, Hello→activity log wiring

Functional implementation (per docs/PRODUCT_REQUIREMENTS.md):
- RulesViewModel: full CRUD + ILocalJsonStore persistence (rules.json). Add/Toggle/Delete. Auto-persist on CollectionChanged.
- ActivityViewModel: reads activity.json, recomputes stat tiles, exposes 5-most-recent events.
- DashboardViewModel: real counts from protectedApps.json + activity.json + rules.json. Latest 5 challenges surfaced to a new Recent activity items control.
- SettingsViewModel: added AuthMethodOption enum (Biometric/PinFallback), DefaultAuthMethod property, persisted via composite PersistentSettings record.
- IActivityLogger / ActivityLogger: on-device persistence of auth outcomes. Serialised via SemaphoreSlim, capped at 200 events.
- BiometricAuthService: 5-arg ctor (added IActivityLogger). Fire-and-forget LogAsync after each outcome (cancellationToken forwarded, CA2016).
- ProcessWatcher: 8-arg ctor (added IActivityLogger). Logs every protected-app launch with INFO + Verified or BLOCKED + outcome.

UI overhaul:
- EntranceAnimation motion util: 320 ms staggered fade-up on Page Loaded, honours Motion.RespectReducedMotion.
- All 7 page code-behinds hook Loaded -> EntranceAnimation.Play(rootPanel/Grid).
- FocalCard / ProtectedAppsPage body / ActivityPage / RulesPage / DashboardPage Recent / SettingsPage rows glassmorphic over IndigoGlow + EmeraldGlow ambient.
- Pages background Painted Transparent so the MainWindow atmosphere bleeds through.
- RulesPage.xaml rewritten with composer + ListBox + EmptyState + per-row Toggle + Delete.
- DashboardPage.xaml Recent activity ItemsControl + CountToVisibility swap.

Tests / build:
- 0 errors, 0 warnings.
- 7/7 auth tests pass; BiometricAuthServiceTests.CreateSut expanded with FakeActivityLogger for activity-event assertions.
- Type alias `using ActivityEvent = BioCentri.App.Types.ActivityEvent;` in ProcessWatcher to avoid System.Diagnostics clash.

<Border x:Class="BioCentri.App.Components.Inputs.SegmentedControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{StaticResource Brushes.Ink.850}"
        BorderBrush="{StaticResource Brushes.Border.Hairline}"
        BorderThickness="1"
        CornerRadius="{StaticResource Corners.Pill}"
        Padding="2"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <ItemsControl x:Name="ItemsHost">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <TextBlock x:Name="Segment"
                           Text="{Binding}"
                           Padding="14,6"
                           FontFamily="{StaticResource Typography.Sans}"
                           FontSize="{StaticResource Typography.Size.Small}"
                           FontWeight="{StaticResource Typography.Weight.Medium}"
                           Foreground="{StaticResource Brushes.Text.Muted}"
                           VerticalAlignment="Center"
                           Cursor="Hand"
                           TextAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</Border>
